
/**
 * @type {string}
 * @description This constant contain all the App roles
 */
const ROLES = {
    "/dashboard": ["admin", "staff"],
    "/sales": ["billing", "sale"],
    "/settings": ["admin", "staff"],
    "/settings/profile": ["admin", "staff"],
    "/settings/account": ["admin", "staff"],
    "/settings/security": ["admin", "staff"],
}

export default ROLES;